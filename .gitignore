node_modules/
.env
*.env.local

# SQLite database + WAL checkpoint shards — runtime data, not for VCS.
# The db was tracked up to the commit that added this line and carried a real
# admin password hash. It is untracked now, but the old contents remain in git
# history: rotate any password/key that ever lived in it, and rewrite history if
# the repository is or becomes public.
data/*.db
data/*.db-wal
data/*.db-shm

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
