node_modules/
.env
*.env.local

# SQLite database + WAL checkpoint shards — runtime data, not for VCS.
# These were tracked once and are no longer; the rows that were in that file
# stay in history until history is rewritten, so treat anything it held as
# disclosed.
data/*.db
data/*.db-wal
data/*.db-shm

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
